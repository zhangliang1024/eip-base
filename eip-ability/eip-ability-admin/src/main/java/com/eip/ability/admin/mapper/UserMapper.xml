<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eip.ability.admin.mapper.UserMapper">

    <select id="findPage" resultType="com.eip.ability.admin.domain.vo.UserResp">
        SELECT
        *
        FROM t_user
    </select>
    <select id="list" resultType="com.eip.ability.admin.domain.entity.baseinfo.User">
        SELECT
        *
        FROM t_user
    </select>

</mapper>
